# Optional. The app works without an API key using deterministic mock planning.
# When set, the agent uses the OpenAI Responses API for intent and slot extraction.
OPENAI_API_KEY=sk-your-key-here
OPENAI_MODEL=gpt-4.1-mini
